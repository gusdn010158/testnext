import React from "react";
import Headers from "@/app/Header/Headers";
import "./globals.css";
import Footer from "./Footer/Footer";

import "./globals.css";
import { Roboto, Noto_Sans_KR } from "next/font/google";

const roboto = Roboto({
  subsets: ["latin"],
  weight: ["400", "500", "700"],
  variable: "--font-roboto",
});

const noto_sans_kr = Noto_Sans_KR({
  subsets: ["latin"],
  weight: ["400"],
  variable: "--font-noto_sans_kr",
});

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children, withHeaderAndFooter = true }) {
  return (
    <html lang="en">
      <body className={`${roboto.variable} ${noto_sans_kr.variable}`}>
        {withHeaderAndFooter && <Headers />}
        {children}
        {withHeaderAndFooter && <Footer />}
      </body>
    </html>
  );
}
